# ── Core ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-dotenv>=1.0.0
httpx>=0.27.0
# ── Gemini LLM ────────────────────────────────────────
google-generativeai>=0.7.0            # Google Gemini API client
pydub
# ── Speech-to-Text ────────────────────────────────────
SpeechRecognition>=3.10.0
PyAudio>=0.2.14            # For microphone access (may need portaudio system package)

# ── Text-to-Speech ────────────────────────────────────
edge-tts>=6.1.10           # Microsoft Neural Voices (free, high quality)
# pyttsx3>=2.90            # Fallback TTS (offline)
yfinance
# ── Optional: Local Whisper STT (higher accuracy) ─────
# Uncomment if USE_WHISPER=true:
faster-whisper>=1.0.0

# ── Optional: Better audio playback on Windows ────────
# pygame>=2.5.0

# ── Dev ───────────────────────────────────────────────
# pytest>=8.0.0
# pytest-asyncio>=0.23.0


# ── Email Agent ───────────────────────────────────────
# imaplib  — stdlib, already included in Python (no install needed)
# smtplib  — stdlib, already included in Python (no install needed)
# email    — stdlib, already included in Python (no install needed)
# All email agent functionality works with zero new packages.